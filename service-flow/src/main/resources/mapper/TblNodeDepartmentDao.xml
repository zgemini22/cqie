<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zds.flow.dao.TblNodeDepartmentDao">
    <insert id="insertList" parameterType="com.zds.flow.po.TblNodeDepartment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tbl_node_department (process_id, due_day, node_id, department_id)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.processId}, #{item.dueDay}, #{item.nodeId}, #{item.departmentId})
        </foreach>
    </insert>
    <update id="batchUpdate" parameterType="com.zds.flow.po.TblNodeDepartment">
        <foreach collection="list" item="item" index="index" separator=";">
            UPDATE tbl_node_department
            SET
            due_day = #{item.dueDay},
            department_id = #{item.departmentId}
            WHERE node_id = #{item.nodeId}
        </foreach>
    </update>
</mapper>
